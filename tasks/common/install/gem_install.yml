---
- name: Install fluentd gem
  become: true
  command: "gem install fluentd -v {{ gem_version }}"
  when: install_type == "gem"

- name: Execute fluentd setup
  become: true
  command: "{{ _exe_path }}/fluentd --setup {{ _config_dir }}"
  when: install_type == "gem"
